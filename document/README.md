把要在文档页展示的文件放进这个文件夹下的分类子目录。

- 每个子文件夹就是一个分类，例如 `document/langchain/`
- 文档页会自动读取分类名；新建文件夹即可新增分类，无需改代码
- 支持格式：md、pdf、txt、html
- Markdown / 文本会在站内阅读器中打开；pdf 和 html 仍打开原文件
- 放好后刷新页面即可
- 文件名会显示为标题，例如 `协作笔记.md`
- 名为 README 的文件不会出现在站点上
